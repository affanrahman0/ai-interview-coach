# PHASE 6: Local dev parity - backend + Postgres in containers.
# Uncomment and adjust once the backend Dockerfile is added.

# version: "3.9"
# services:
#   db:
#     image: postgres:16
#     restart: always
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: interview_coach
#     ports:
#       - "5432:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data
#
#   backend:
#     build: ./backend
#     command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
#     env_file:
#       - ./backend/.env
#     ports:
#       - "8000:8000"
#     volumes:
#       - ./backend:/app
#     depends_on:
#       - db
#
# volumes:
#   pgdata:
